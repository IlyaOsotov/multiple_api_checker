RSpec.describe MultipleApiChecker do
  it "has a version number" do
    expect(MultipleApiChecker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
